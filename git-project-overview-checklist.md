以下に、文章の内容を**知見として整理したMarkdown形式**でまとめました👇

---

# 🧭 新規プロジェクト参画時に使うGitコマンド5選

新しくプロジェクトに参加した際、コードを読む前に**リポジトリの全体像を素早く把握するためのGitコマンド**をまとめる。

参考：*The Git Commands I Run Before Reading Any Code*

---

## ① 変更頻度の高いファイルを特定する

```bash
git log --format=format: --name-only --since="1 year ago" \
| sort | uniq -c | sort -nr | head -20
```

### 🎯 目的

* 活発に開発されている箇所の特定
* 変更が多く影響範囲の広いファイルの把握

### 💡 知見

* 変更頻度が高い = 重要 or 問題が多い箇所
* 誰も責任を持ちたがらないファイルの変更が多い場合
  → **コードベースの停滞の兆候**

---

## ② コードの主要貢献者を把握する（バスファクター）

```bash
git shortlog -sn --no-merges
```

### 🎯 目的

* 誰がプロジェクトを主導しているか把握
* 属人化リスクの確認

### 💡 知見

* 特定の人物がコミットの大半（例：60%以上）
  → **バスファクターが高く危険**
* 併用推奨：

```bash
git shortlog -sn --no-merges --since="6 months ago"
```

→ 最近のアクティブメンバーを把握

### ⚠️ 注意

* squashマージ（PRを1コミットにまとめる）環境では参考になりにくい

---

## ③ バグが集中している箇所を特定する

```bash
git log -i -E --grep="fix|bug|broken" --name-only --format='' \
| sort | uniq -c | sort -nr | head -20
```

### 🎯 目的

* バグ修正が多いファイルの特定

### 💡 知見

* 頻出するファイルは：

  * 根本的に設計が悪い
  * パッチ対応ばかりで改善されていない

### ⚠️ 前提

* コミットメッセージが適切に書かれている必要あり

---

## ④ プロジェクトの勢い（開発速度）を把握する

```bash
git log --format='%ad' --date=format:'%Y-%m' | sort | uniq -c
```

### 🎯 目的

* 月ごとのコミット数から開発の活発さを分析

### 💡 知見

* 安定している → 健全
* 急減 → メンバー離脱の可能性
* 長期減少（6ヶ月以上） → **チームの勢い低下**
* 定期的な急増 → **バッチリリース型開発**

---

## ⑤ 障害・炎上（ロールバック頻度）を把握する

```bash
git log --oneline --since="1 year ago" \
| grep -iE 'revert|hotfix|emergency|rollback'
```

### 🎯 目的

* 緊急対応・巻き戻しの頻度を確認

### 💡 知見

* 年数回 → 正常範囲
* 数週間ごと → **重大な問題の兆候**

考えられる原因：

* テストの信頼性不足
* ステージング環境の欠如
* デプロイパイプラインの問題

### ⚠️ 注意

* ヒットしない場合：

  * 安定している
  * or コミットメッセージが不適切

---

# 🧠 まとめ（実務での使い方）

これら5つのコマンドにより、コードを読む前に以下を把握できる：

* 🔥 どこがよく変更されているか
* 👤 誰がプロジェクトを支えているか
* 🐛 バグの温床はどこか
* 📈 開発の勢いはどうか
* 🚨 障害対応の頻度はどれくらいか

👉 **最初の1時間でプロジェクトの健康状態を俯瞰できる**

---

必要なら「これをNotionテンプレ化」や「実務チェックリスト化」も作れるので言ってください 👍
